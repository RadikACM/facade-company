export interface ContactData {
  phone: string;
  phoneWp: string;
  email: string;
  address: string;
  adressUrl: string;
  workingHours: string;
  instagramUrl?: string;
  telegramUrl?: string;
  whatsappUrl?: string;
  facebookUrl?: string;
}
